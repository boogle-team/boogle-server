describe('buildMonthlyPdfData', () => {
  it('KST 날짜를 기준으로 월간 기록을 일별 행에 병합한다', () => {
    // 2026-07-01T00:30:00+09:00은 UTC로 전날이지만 7/1 행이어야 한다.
  });

  it('시작일부터 종료일까지 기록 없는 날을 포함한다', () => {
    // 7/1~7/3이면 dailyRows.length === 3
  });

  it('변 상태 분포를 보통, 딱딱, 묽음 순서로 계산한다', () => {
    // M 2건, H 1건, T 1건 -> 50%, 25%, 25%
  });

  it('불편감 기록 수와 동반 최빈 변 상태를 계산한다', () => {
    // stomach M/L 기록만 집계하고 N/null은 제외
  });

  it('하루 여러 배변 기록의 마지막 변 상태와 최대 불편감을 표시한다', () => {
    // 오전 M, 오후 L이면 오후 변 상태와 심함 표시
  });

  it('수면, 수분, 스트레스의 세 단계 일수를 집계한다', () => {
    // 각각 B/N/G, L/N/H, L/N/H 매핑
  });

  it('음식 태그를 횟수 내림차순과 이름 오름차순으로 정렬해 2개만 선택한다', () => {
    // 동률 결과도 항상 같아야 한다.
  });

  it('패턴 카드가 없으면 빈 배열을 유지한다', () => {
    // 렌더러가 섹션을 생략할 수 있어야 한다.
  });
});
