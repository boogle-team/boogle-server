# Prisma 클라이언트를 커스텀 경로(src/generated/prisma)에 생성하는데, pnpm의
# 격리형 node_modules에서는 생성 코드가 의존하는 @prisma/client-runtime-utils가
# 최상위 node_modules로 올라오지 않아 런타임 모듈 해석이 실패한다(특히 Windows).
# prisma 관련 패키지를 최상위로 hoist해 어느 경로에서든 해석되도록 한다.
public-hoist-pattern[]=*prisma*
